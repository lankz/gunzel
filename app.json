{
  "name": "Gunzel",
  "description": "Slices through the setup time for your next Rails project",
  "repository": "https://github.com/lankz/gunzel.git",
  "env": {
    "SECRET_KEY_BASE": {
      "generator": "secret"
    }
  },
  "scripts": {
    "postdeploy": "bundle exec rake db:migrate --trace"
  },
  "success_url": "/",
  "addons": [
    "heroku-postgresql:hobby-dev"
  ]
}
